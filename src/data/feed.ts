import { FeedItem } from "@/components/meku/FeedCard";

/**
 * Real data only. Feed starts empty until the user creates a post
 * or follows accounts that have posted.
 */
export const feedItems: FeedItem[] = [];
